

{
  "watch": ["src", "server"],
  "ignore": ["src/**/*.test.ts", "public"],
  "ext": "tsx,js,json,css,scss",
  "exec": "rm -rf public/* && npm run build && npm run start",
  "legacyWatch": true,
  "delay": 500
}